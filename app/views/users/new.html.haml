%h2 New User

= form_for @user,:url => create_user_path, :method => :post, :html => { :multipart => true, :id => "add_user"} do |f|
  = render :partial => "form", :locals => { :f => f, :user => @user }